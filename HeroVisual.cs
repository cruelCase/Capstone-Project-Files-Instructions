using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class HeroVisual : MonoBehaviour
{
    [Header("Assign These")]
    public Image heroImage;      // The UI Image that will display the hero sprite
    public TMP_Text heroName;    // The TMP text showing the hero's name

    // Optional: If you want animation or extra logic later
    // you can add more variables here.
}
